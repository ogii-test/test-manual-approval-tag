version: 2.1

tag-filter: &tag-filter
  tags:
    only: /^v.*/
  branches:
    ignore: /.*/

jobs:
  build:
    docker:
      - image: cimg/base:2021.07
    steps:
      - checkout
      - run: echo "test"

workflows:
  test-workflow:
    jobs:
      - build:
      - hold:
          type: approval
          requires:
            - build
      - build:
          name: build-after-approval
          requires:
            - hold
